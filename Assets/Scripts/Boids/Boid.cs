using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Boid : MonoBehaviour
{
    /*
     * list of transforms for boids near me
     *    - List<Transform>
     * 
     * list of raycast directions that missed (open paths)
     *    - List<Vector3>
     * 
     * list of raycasts that hit a obstacle (OPTIONAL)
     *    - List<RaycastHit>
     *    
     *  Debug.DrawRay()
     */
}
